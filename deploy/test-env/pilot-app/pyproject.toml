[project]
name = "pilot-app"
version = "0.1.0"
description = "AI SDLC 平台 E2E 试点应用"
requires-python = ">=3.12"
dependencies = ["fastapi>=0.115", "uvicorn>=0.30"]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.pytest.ini_options]
testpaths = ["tests"]
